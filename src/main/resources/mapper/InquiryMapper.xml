<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.study.project05.inquiry.mapper.InquiryMapper">

    <!-- 공통 검색 조건 -->
    <sql id="inquirySearchCondition">
        <where>
            <if test="inquiryVO.statusFilter != null and inquiryVO.statusFilter != ''">
                i.i_status = #{inquiryVO.statusFilter}
            </if>
            <if test="inquiryVO.searchWord != null and inquiryVO.searchWord != ''">
                AND (i.i_title LIKE CONCAT('%', #{inquiryVO.searchWord}, '%')
                  OR u.u_name  LIKE CONCAT('%', #{inquiryVO.searchWord}, '%'))
            </if>
        </where>
    </sql>

    <!-- 전체 문의 수 -->
    <select id="getInquiryCount" parameterType="map" resultType="int">
        SELECT COUNT(*)
          FROM inquiries i
          LEFT JOIN user u ON i.u_idx = u.u_idx
        <include refid="inquirySearchCondition"/>
    </select>

    <!-- 문의 목록 조회 (미답변 우선 → 최신순) -->
    <select id="getInquiryList" parameterType="map" resultType="InquiryVO">
        SELECT i.i_idx       AS inq_idx,
               i.u_idx       AS user_idx,
               u.u_name      AS user_name,
               i.i_category  AS inq_category,
               i.i_title     AS inq_title,
               i.i_status    AS inq_status,
               DATE_FORMAT(i.i_created,  '%Y-%m-%d %H:%i') AS inq_created,
               DATE_FORMAT(i.i_answered, '%Y-%m-%d %H:%i') AS inq_answered
          FROM inquiries i
          LEFT JOIN user u ON i.u_idx = u.u_idx
        <include refid="inquirySearchCondition"/>
         ORDER BY
            CASE WHEN i.i_status = 'PENDING' THEN 0 ELSE 1 END ASC,
            i.i_created DESC
         LIMIT #{numPerPage} OFFSET #{offset}
    </select>

    <!-- 문의 단건 상세 조회 (답변 포함) -->
    <select id="getInquiryDetail" parameterType="String" resultType="InquiryVO">
        SELECT i.i_idx       AS inq_idx,
               i.u_idx       AS user_idx,
               u.u_name      AS user_name,
               i.i_category  AS inq_category,
               i.i_title     AS inq_title,
               i.i_status    AS inq_status,
               i.i_content   AS inq_content,
               i.i_file_url  AS inq_file_url,
               i.i_answer    AS inq_answer,
               DATE_FORMAT(i.i_created,  '%Y-%m-%d %H:%i') AS inq_created,
               DATE_FORMAT(i.i_answered, '%Y-%m-%d %H:%i') AS inq_answered
          FROM inquiries i
          LEFT JOIN user u ON i.u_idx = u.u_idx
         WHERE i.i_idx = #{inqIdx}
    </select>

    <!-- 답변 저장 + 상태 자동 변경 -->
    <update id="saveAnswer" parameterType="map">
        UPDATE inquiries
           SET i_answer   = #{inqAnswer},
               i_status   = 'COMPLETE',
               i_answered = NOW()
         WHERE i_idx = #{inqIdx}
    </update>

    <!-- 미답변 문의 수 (대시보드 알림용) -->
    <select id="getPendingCount" resultType="int">
        SELECT COUNT(*) FROM inquiries WHERE i_status = 'PENDING'
    </select>

</mapper>
